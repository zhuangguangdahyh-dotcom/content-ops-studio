# Roadmap

Stage 11 is the final V1 release-engineering and public-release stage. It freezes version `0.1.0`, validates a whitelisted package through repository-external clean install and installed E2E, and publishes the MIT-licensed source and release artifact. It does not add features.

Stage 10 is complete: Finalization Runtime V1, immutable Final Manifest, deterministic Final Set Fingerprint, approved-only Delivery Package, integrity verification, versioned Archive, partial-failure recovery, idempotent replay and Plugin V1 E2E smoke all pass. Image Production Skill V1 remains `PRODUCTION_READY / FROZEN`; no visual feature was added. Feishu metadata sync remains explicit and partial because dedicated Manifest ID/Fingerprint fields are absent, while attachment upload remains deferred without blocking local Finalization.

Phase 4E is implemented: production rendering now has pinned fresh-context determinism, actual-font Text Layout Preflight, bounded semantic layout recovery, isolated final-raster contrast analysis, Copy/Graphic Separation and formal promotion gates. The immutable Phase 4D A/B sources and strategies were rerun without ImageGen; both six-page sets pass with zero Hard Blocks while Phase 4D remains `FAILED` and byte-unchanged. The full repository check passed, so `IMAGE_PRODUCTION_SKILL_V1` is `PRODUCTION_READY / FROZEN_FOR_V1`; this does not authorize G4/G5, Feishu writes, publishing or a Universal visual template.

Phase 4B-R.2.6 is implemented: the Operator-approved, unchanged calibration FPV-2 now has an exact G4 approval, Calibration Style Lock V1 and `CALIBRATION_VALIDATED_V1` rule/knowledge/QA evidence. Remaining-page production is eligible but unexecuted; no current Cover layout is a Universal template, no Feishu write occurred and Phase 4C has not started.

Phase 4B-R.2.5 is implemented: actual-raster local contrast, stability and background-complexity hard-gate every formal text layer before attention scoring. FPV-1 remains preserved as a quality-defect reference; FPV-2 passed full/310/186 actual-pixel and deterministic evidence before the later explicit Calibration G4 approval.

Phase 4B-R.2.4 has produced the first formal fictional commercial-space calibration Cover from selected Candidate I through a fully re-run pipeline. It passes all technical and visual gates and is paused at `CALIBRATION_G4 / AWAITING_USER_APPROVAL`; no approval, formal Style Lock, remaining page, Feishu write or long-term template exists.

Phase 4B-R.2.3 is implemented: sourced non-template Editorial Design Knowledge, page-intent separation, dynamic Cover Attention modes, visual-mass and grayscale-first color intelligence, typography-as-form and a post-spatial/pre-G4 attention gate now pass offline and Round 4 calibration. A–H are preserved; I/J/K remain unselected with no formal FPV/G4/Style Lock, remaining pages or Feishu writes.

Phase 4B-R.2.2 is implemented: Typography Spatial Integrity and relative Typography Breathing Room now hard-gate Visual Quality using real Renderer geometry and actual-pixel inspection. Historical A–F remain immutable; new G/H calibration assets remain at Operator direction selection with no formal FPV/G4/Style Lock or Feishu write.

Phase 2B.2 introduced the official CLI default path. Phase 2C now implements and validates the bundled local STDIO MCP exposure. Research, image generation, Renderer, attachments, public MCP hosting and publishing remain deferred.

Phase 2B delivers offline-complete Feishu project initialization. Phase 2B.1 is the sandbox evidence gate when credentials are available. Only after that evidence passes should Phase 2C expose existing Runtime/Adapter operations through an MCP wrapper; attachment, research, images, rendering and publishing remain later independent phases.

## Phase 1: contract hardening (implemented)

Core domain and visual/finalization contracts, generated TypeScript, migrations, deterministic state/approval/invalidation logic, Adapter contracts, fixtures, tests, and the Node 24 LTS (`>=24 <25`) Runtime baseline are implemented.

## Phase 2: local deterministic runtime

Compose project registry, pack loader, capability discovery, checkpointing, write logs, and the Phase 1B temporary Asset Store/Renderer planning contracts into a recoverable local runtime behind mocks. Do not add production providers.

## Phase 3: external Adapter implementation

After accepted ExecPlans, implement Feishu workspace operations, host research, image generation, and deterministic rendering with explicit approvals and verification reads.

Phase 2B may introduce production project initialization and a Feishu Workspace Adapter only under a separate accepted ExecPlan. It must not reinterpret Node 24 Runtime success or Phase 2A Mock writes as external integration success. Cross-platform CI remains useful release evidence but is not a Node 20 or Phase 2B gate.

## Phase 3

Phase 3A provides evidence-backed painpoint research and G2. Phase 3B implements evidence-grounded `content-creation`, one-row Feishu Content persistence, quality/duplication gates, G3 and revision planning. Its first Live package passed explicit G3 approval, remote read verification and idempotent replay. It does not add image generation, rendering, attachments, publishing or a public MCP endpoint.

## Phase 4A

Phase 3B G3, replay and final evidence are complete. Phase 4A may implement the formal `visual-planning` business workflow. G3 approval did not and must not auto-start it.

## Phase 4: production QA and Plugin distribution

Add visual testing, real integration fixtures in a non-customer test workspace, security review, evals, release metadata, installation tests, and an explicit publish decision.
Phase 4A completes formal planning and the first-page handoff. Next is controlled Cover production, explicit G4 and Style Lock; production Renderer, remaining images, attachments, final QA/publishing and public MCP hosting remain later milestones.

# Phase boundary after 4B

Phase 4B-R completes Image Production Specification V1, Host-native generation handoff, direction selection and quality/learning contracts. Current C-0001 pauses at direction selection. After an Operator selects a direction, the existing future path is Visual Plan revision → formal first page → G4 → Style Lock → remaining pages → Group QA/G5. Attachment upload, publication, Public HTTP MCP and public submission remain later work.

Phase 4B-R.2 hardens Cover conversion, two-size thumbnail QA, click clarity and visual semantic relevance. C-0001 is explicitly routed to Cover Copy revision and its existing FPV-2 is retained only as a current-set negative reference. The separate fictional commercial-space calibration remains at `AWAITING_USER_SELECTION`; it does not authorize a formal FPV, G4, Style Lock, subsequent pages or Phase 4C.
