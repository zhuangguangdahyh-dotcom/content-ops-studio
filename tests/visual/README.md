# Future visual tests

Future checks cover aspect ratio, safe area, text overflow, title placement, type hierarchy, missing pages, output resolution, and full-set consistency. Bootstrap installs no browser and creates no snapshots.
