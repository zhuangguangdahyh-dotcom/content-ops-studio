# Content Ops Studio 纯中文版使用说明

适用版本：`v0.2.0`

项目地址：<https://github.com/zhuangguangdahyh-dotcom/content-ops-studio>

## 一、它能帮你做什么

Content Ops Studio 是一套在 Codex 中运行的本地优先内容生产插件，适合长期管理小红书图文项目。

它可以把一条内容从项目资料整理开始，依次推进到：

`项目初始化 → 痛点研究 → 文案生产 → 视觉规划 → 首图生产 → 整组图片生产 → 最终交付`

系统会保存每个版本、审批记录、图片校验结果和最终交付清单。修改新版本时，不会覆盖已经批准的旧版本。

当前版本主要具备：

- 创建和管理多个内容项目；
- 基于证据研究目标客户痛点；
- 生成并检查小红书图文文案；
- 规划封面和多页视觉系统；
- 生成视觉方向候选；
- 使用图片生成能力制作底图；
- 使用浏览器渲染器排版正式中文；
- 完成首图、整组图片和移动端可读性检查；
- 通过五个人工审批节点控制质量；
- 生成不可篡改的最终交付清单、指纹和归档；
- 可选连接飞书多维表格。

它不会自动替你发布内容，也不会绕过人工审批。

## 二、使用前需要准备什么

### 必需条件

- macOS、Linux 或受支持的本地 Codex 环境；
- Node.js `24`，版本范围必须是 `>=24 <25`；
- Codex 桌面端或支持插件的 Codex 命令行；
- 足够的本地磁盘空间保存项目文件和图片。

### 按需准备

- 需要正式渲染图片时：安装与插件匹配的 Chromium；
- 需要生成图片底图时：当前 Codex 环境需要提供图片生成能力；
- 需要同步飞书时：准备一个可用的飞书账号并完成官方浏览器授权；
- 不连接飞书时：仍可进行本地测试、合同验证和本地交付。

普通使用不需要把 OpenAI API Key 写进插件，也不要把飞书密钥粘贴到对话里。

## 三、推荐安装方式

### 方式一：从公开仓库安装到 Codex

在终端中执行：

```bash
codex plugin marketplace add zhuangguangdahyh-dotcom/content-ops-studio --ref v0.2.0
codex plugin list --available
codex plugin add content-ops-studio@content-ops-studio-local
codex plugin list
```

看到以下信息即表示安装成功：

- 插件名称：`content-ops-studio`；
- 版本：`0.2.0`；
- 状态：已安装、已启用。

安装后请新建一个 Codex 任务。已经打开的旧任务不一定会自动加载刚安装的技能和工具。

如果使用 Codex 桌面端，可以在终端完成上述安装，然后重新打开 Codex 或新建任务。

### 方式二：从本地源码安装

先下载和构建源码：

```bash
git clone --branch v0.2.0 --depth 1 https://github.com/zhuangguangdahyh-dotcom/content-ops-studio.git
cd content-ops-studio
corepack enable
pnpm install --frozen-lockfile
pnpm mcp:build
pnpm validate:plugin
pnpm plugin:package-test
```

再把当前仓库注册为本地插件市场：

```bash
codex plugin marketplace add .
codex plugin add content-ops-studio@content-ops-studio-local
codex plugin list
```

### 方式三：只验证源码，不安装插件

适合开发者或想先确认环境是否正常的人：

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm plugin-v1:e2e
```

这项测试只使用隔离的虚构数据，不会写飞书、不会调用图片生成、不会发布内容。

## 四、第一次启动

新建 Codex 任务后，直接发送：

```text
请使用 content-ops-studio 检查当前运行环境，只执行 Doctor，不创建项目，不写飞书。
```

正常情况下，系统会检查：

- Node.js 是否为 24；
- 插件数据目录是否与插件安装目录隔离；
- 72 个 MCP 工具是否可用；
- 飞书是否已经授权；
- 渲染器是否已经准备好；
- 当前是否存在可恢复的项目或运行任务。

如果 Doctor 报错，先按它给出的下一步修复，不要直接开始正式项目。

## 五、创建第一个项目

你不需要手动调用工具。直接把真实业务信息告诉 Codex，并要求它先做计划、不要立即写入。

可以复制下面这段：

```text
请使用 content-ops-studio 创建一个新内容项目。

项目名称：
项目主体：
主体类型：
所属行业：
细分领域：
服务地域：
目标客户：
核心优势：
目标平台：小红书

请先检查信息缺口并生成初始化计划，不要自动批准G1，不要开始痛点研究。
```

系统会先生成初始化计划。如果缺少会影响后续工作的关键信息，它会集中询问，而不是直接猜测。

确认计划后，可以回复：

```text
我确认当前项目初始化计划，允许执行项目初始化。完成后停在G1，等待我确认。
```

检查项目资料无误后，再明确回复：

```text
我确认当前项目配置，执行G1 APPROVE。批准后恢复当前Run并验证Workspace。
```

G1 通过后，项目才会进入可正式使用状态。

## 六、完整内容生产流程

### 第一步：痛点研究和G2

可以发送：

```text
请基于当前项目资料和公开证据，研究目标客户的真实决策痛点。
先生成研究计划，再检索和保留必要证据。
不要为了凑数量生成弱痛点，不要自动执行G2审批。
```

系统会：

1. 读取当前项目资料；
2. 制定研究计划；
3. 通过 Codex 当前环境提供的研究能力查找来源；
4. 保存必要的来源摘要和证据关系；
5. 生成并评分痛点候选；
6. 停在 G2，等待逐条判断。

G2 必须逐条给决定。示例：

```text
确认按以下结果执行G2：
P-0001 APPROVE
P-0002 APPROVE
P-0003 REVISE：需要更聚焦成交前的判断障碍
P-0004 REJECT：与现有痛点重复
```

只有 `APPROVE` 的痛点才能进入正式内容生产。

### 第二步：文案生产和G3

可以发送：

```text
请基于已批准的P-0001创建一组小红书图文文案。
先提供至少三个真正不同的内容角度，再选择最适合当前目标客户的一种。
保持一个核心痛点、一条核心判断、4—8页完整结构。
完成文案和质量检查后停在G3，不要自动批准。
```

系统会分别保存：

- 内容版本；
- 文案版本；
- 封面转化文案版本；
- 每页承担的职责；
- 事实与证据关系；
- 重复度和质量报告。

审核时不要只说“可以”。建议精确绑定当前版本：

```text
确认批准C-0001当前CV-1、Copy Version CV-1和当前Cover Copy Version，执行G3 APPROVE。
```

任何正文、页数或封面文案修改都会产生新版本，旧 G3 不再授权新版本。

### 第三步：视觉规划

G3 通过后，可以发送：

```text
请基于当前已批准文案制定整组视觉计划。
先给出至少三个有实质差异的视觉方向，说明每个方向的内容匹配优势、审美风险和手机缩略图表现。
不要生成正式图片，不要自动选择方向。
```

视觉计划会分别定义：

- 封面职责；
- 中间内容页职责；
- 最后一页总结与转化职责；
- 每页底图的语义作用；
- 同组视觉连续性；
- 字体、色彩、留白和版式规则；
- 图片素材需求；
- 手机端阅读要求。

选择方向时要明确候选编号：

```text
确认选择候选A作为当前内容的正式视觉方向。
本次选择只作用于当前整组，不写入长期项目偏好。
请创建新Visual Plan Version并进入正式首图生产。
```

### 第四步：正式首图和G4

首次生产正式图片前，先发送：

```text
请检查Renderer状态。如果未安装匹配的Chromium，先给出安装计划并等待我确认；不要使用系统Chrome或Mock替代正式Renderer。
```

确认安装后，再要求生产正式首图：

```text
请按照已选视觉方向生产正式首图。
图片底图可以使用Host图片生成能力，但所有正式中文必须由Renderer排版。
完成机械质量、视觉质量、项目适配和手机缩略图检查后展示真实PNG，并停在G4。
```

系统应展示：

- 真实首图；
- 内容、文案、视觉计划和首图版本；
- 图片尺寸、文件大小和完整校验值；
- 机械质量结果；
- 视觉质量结果；
- 项目适配结果；
- 当前仍存在的审美风险；
- G4 批准后准备锁定的规则。

满意后再明确批准：

```text
我确认当前展示的正式首图，按当前Content、Copy、Visual Plan、First Page Version、Asset ID和SHA-256执行G4 APPROVE。
批准后创建当前整组的Style Lock，不要自动生成剩余页面。
```

质量分通过不等于 G4 通过。只有你的明确批准才能创建 Style Lock。

### 第五步：生产剩余页面和G5

G4 和 Style Lock 完成后，可以发送：

```text
请根据当前Style Lock生产剩余页面。
同组保持统一视觉语言，但每页使用与页面职责匹配的不同底图、机位或构图。
中间页负责价值输出，最后一页负责总结和自然承接转化。
完成单页检查、整组连续性检查和Contact Sheet后停在G5，不要自动批准。
```

检查整组时重点看：

- 六张图是否像同一个视觉项目；
- 每页是否使用了适合本页职责的素材；
- 是否只是重复同一张底图或同一机位；
- 封面是否有点击理由；
- 中间页是否真正交付价值；
- 最后一页是否先总结，再自然承接咨询；
- 手机端能否读清核心中文；
- 是否存在错字、乱码、溢出、裁切或低对比文字。

确认整组后，执行精确的 G5：

```text
我确认当前完整图片组及其全部版本和SHA-256，执行G5 APPROVE。不要修改任何已批准图片。
```

### 第六步：最终交付

G5 通过后，可以发送：

```text
请为当前已通过G5的图片组执行Finalization。
重新读取全部PNG和Checksum，生成Final Manifest、Final Set Fingerprint、Delivery Package、Integrity Report和Archive。
对最终PNG执行隐私元数据脱敏，删除caBX等隐私块，但不得重新编码像素。
不要重新生成图片，不写飞书附件，不自动发布。
```

最终交付应包含：

- 只含批准版本的图片文件；
- 最终清单；
- 整组唯一指纹；
- 完整性报告；
- 版本化归档；
- 可追踪的审批链。
- PNG元数据脱敏报告，证明分辨率、位深、色彩类型和IDAT像素流未改变。

需要保存到指定文件夹时，再明确发送：

```text
请把当前已Finalized的脱敏PNG导出到：<绝对文件夹路径>。
只导出最终页，不导出候选图、废稿或历史图；不得重新编码像素。
```

系统会在该目录下创建一个由插件管理的内容版本子文件夹。它只清理这个子文件夹内由插件生成的临时文件或过期页名，不会删除其他用户文件。

## 七、飞书连接和使用

飞书是可选能力。普通操作者优先使用官方 Lark CLI 浏览器授权，不需要 App ID 或 App Secret。

在 Codex 中发送：

```text
请使用content-ops-studio检查飞书状态，只执行只读检查，不创建Base，不写记录。
```

如果尚未授权，再发送：

```text
请启动官方飞书授权流程。不得要求我在对话中提供App Secret、Token或Authorization信息。
```

完成浏览器授权后，再让系统重新检查权限和范围。

任何真实飞书写入都必须先：

1. 执行 Doctor；
2. 检查权限；
3. 生成只读计划；
4. 展示计划哈希和预计操作；
5. 获得你的明确写入确认；
6. 写入后重新读取验证。

遇到权限错误时，不要改用其他身份或 Mock 绕过。先修复当前账号授权或管理员权限。

## 八、项目文件保存在哪里

插件安装目录保持只读。正式项目资料不会写进 GitHub 仓库或插件目录。

默认数据位于 Codex 为插件分配的数据目录内：

```text
插件数据目录/
└── content-ops-home/
    └── projects/
        └── 项目ID/
            ├── runs/
            ├── workspace/
            ├── assets/
            ├── delivery/
            └── archive/
```

在源码开发模式下，可以通过 `CONTENT_OPS_HOME` 指向仓库之外的专用目录。不要把它设置为插件目录、仓库目录或真实客户资料的混合目录。

## 九、五个审批节点分别控制什么

- G1：确认项目资料和项目配置；
- G2：逐条批准、修改或拒绝痛点；
- G3：批准精确绑定版本的内容和文案；
- G4：批准精确绑定文件校验值的正式首图，并创建当前整组 Style Lock；
- G5：批准精确绑定全部图片校验值的最终整组。

通用规则：

- 系统不得自动批准；
- 高分不得代替人工批准；
- 新版本不能继承旧版本的审批；
- 修改正文后，需要重新走 G3；
- 修改正式首图后，需要重新走 G4；
- 修改最终整组后，需要重新走 G5；
- `REVISE` 和 `REJECT` 都保留历史，不删除旧资产。

## 十、如何给系统反馈

一次性修改当前内容时，建议明确说：

```text
这是当前整组的一次性生产反馈，只作用于CURRENT_SET，不写入长期项目规则。
```

希望以后同一项目都遵守时，建议明确说：

```text
这是一个项目级长期规则候选。请先保存原始反馈并生成Rule Candidate，展示给我确认；未经我再次确认，不得激活为长期规则。
```

不要把质量缺陷描述成个人审美偏好。例如乱码、裁切、文字碰撞、低对比度、重复底图属于质量问题，不应写入长期审美规则。

## 十一、常见问题

### 1. 安装后找不到插件

先执行：

```bash
codex plugin list
```

确认插件为已安装、已启用，然后新建 Codex 任务。旧任务可能没有加载新插件。

### 2. Node.js 版本不正确

执行：

```bash
node --version
```

必须显示 Node.js 24。Node.js 20、22、25 或更高版本都不属于当前正式支持范围。

### 3. Renderer不可用

在 Codex 中要求执行 Renderer Doctor。只安装插件声明的匹配 Chromium，不要改用系统 Chrome，不要用 Mock 冒充正式渲染。

源码环境也可以执行：

```bash
pnpm renderer:setup
pnpm renderer:doctor
```

### 4. 飞书授权失败

按顺序检查：

1. 官方 Lark CLI 版本；
2. 当前账号浏览器授权状态；
3. 多维表格权限；
4. 目标文件夹或 Base 的访问权限；
5. 管理员是否需要批准应用。

不要把 Token、App Secret 或完整飞书链接发进聊天、日志或 GitHub Issue。

### 5. 审批提示版本过期

重新读取当前 Run 状态和待审批对象，确认最新版本后再批准。不要把旧版本编号手动改成新版本编号。

### 6. 运行中断了

发送：

```text
请读取当前项目Run状态、最后Checkpoint和失败原因。保持原Project ID、Run ID、版本和幂等范围，从最后已验证阶段恢复，不要创建重复项目或重复Base。
```

### 7. 图片质量分通过，但我不喜欢

不要批准 G4 或 G5。直接给出具体审美反馈并要求 `REVISE`。机械质量和评分只能证明图片没有明显技术缺陷，不能替代你的审美判断。

### 8. 为什么不能自动发布

`v0.2.0` 没有自动发布能力。Finalization 只生成本地交付和归档，不会自动发布小红书，也不会假装已经发布。

## 十二、当前版本限制

- 飞书最终元数据同步为 `PARTIAL`；
- 飞书附件上传为 `DEFERRED`；
- 不提供自动发布；
- 不提供公共 HTTP MCP 服务；
- 图片生成依赖 Codex 当前环境提供的图片生成能力；
- 正式渲染依赖匹配版本的 Playwright 和 Chromium；
- 不会自动把一次视觉选择升级为行业规则或全局偏好。

## 十三、卸载或升级

查看已安装插件：

```bash
codex plugin list
```

卸载：

```bash
codex plugin remove content-ops-studio@content-ops-studio-local
```

升级本地市场快照：

```bash
codex plugin marketplace upgrade content-ops-studio-local
```

升级前先查看新版本发布说明。不要用新版本覆盖正在执行中的项目版本和审批链。

## 十四、最短可复制使用指令

如果插件已经安装，可以直接从这段开始：

```text
请使用content-ops-studio执行以下任务：

1. 先运行Doctor，只读检查Node、Plugin Data、Renderer、ImageGen和飞书状态；
2. 如果存在阻塞，只报告准确原因和修复动作，不创建项目；
3. 如果环境正常，为我创建一个新的小红书图文项目；
4. 先收集并确认项目主体、目标客户、行业、地域、核心优势和平台信息；
5. 生成初始化计划，得到我明确确认后再执行；
6. 初始化后停在G1，不自动批准；
7. 后续每个G2、G3、G4、G5都必须展示准确对象、版本和风险，等待我的明确决定；
8. 不自动发布，不上传飞书附件，不把一次性反馈写成长期规则；
9. 所有项目文件保存在插件外部Project Home，保留历史版本和Checksum。
```

## 十五、进一步资料

- 项目简介：[README.md](README.md)
- 快速开始：[QUICK_START.md](QUICK_START.md)
- 环境要求：[ENVIRONMENT.md](ENVIRONMENT.md)
- MCP 工具参考：[docs/20-mcp-tool-reference.md](docs/20-mcp-tool-reference.md)
- 本地插件安装：[docs/21-local-plugin-installation.md](docs/21-local-plugin-installation.md)
- 飞书授权：[docs/17-lark-cli-user-setup.md](docs/17-lark-cli-user-setup.md)
- 安全规则：[SECURITY.md](SECURITY.md)
- 隐私规则：[PRIVACY.md](PRIVACY.md)
- GitHub Release：<https://github.com/zhuangguangdahyh-dotcom/content-ops-studio/releases/tag/v0.2.0>
