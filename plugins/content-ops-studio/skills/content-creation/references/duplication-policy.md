# Duplication policy

Core owns the deterministic fingerprint. The host owns retained near-semantic reasoning and must not claim embeddings. HIGH blocks; MEDIUM needs a differentiation rationale; LOW proceeds. Same-idempotency replay is reuse. A different request with the same fingerprint cannot create another Content.
