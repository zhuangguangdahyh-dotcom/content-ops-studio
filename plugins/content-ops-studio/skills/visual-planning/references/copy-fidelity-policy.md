# Copy fidelity policy

Hash exact NFKC snapshots per page. Wrapping, emphasis and layer splitting are presentation only; the approved headline/body/supporting text must remain exact. Copy or page-count drift returns to Content Creation and requires a new G3.
