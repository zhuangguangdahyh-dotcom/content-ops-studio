# Revision policy

Visual-only change increments `VV-N`, retains the prior plan and needs neither Content/Copy version change nor new G3. Copy/page-count changes set `requires_content_revision` and `requires_new_g3`. Phase 4A has no produced Cover or G4, so regeneration/new-G4 flags remain false.
