# Workflow

Doctor → context → direction dry run → Host-authored Visual System/pages/references/assets/layout/quality → local submit → explicit finalize → remote read verification → plan verify → first-page handoff. Stop after handoff.
