# Contract

Use the eight Phase 4A contracts and reuse canonical `visual-system`/`page-visual-plan`. Bind Content, Copy and Visual Plan versions, Run and exact page hashes. Closed roots and controlled `extensions` are mandatory; absolute paths, secrets, G4 and generated assets are invalid.
