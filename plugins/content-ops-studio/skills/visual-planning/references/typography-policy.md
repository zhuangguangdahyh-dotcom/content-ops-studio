# Typography policy

Use portable system-resolvable Chinese sans-serif stacks. Token IDs are unique and every Text Layer resolves to a token. Maintain display title, page title, body, supporting, number, caption and page-number roles through the token system. Never submit font binaries or shrink copy to unreadable size.
