# Failure handling

Return stable errors for missing/stale G3, non-approved Content, copy/page drift, prohibited mode/reference, unavailable asset, missing token, overflow, layout/quality block, stale/conflicting plan and incomplete handoff. Preserve artifacts and retry only the missing verified step; never delete history.
