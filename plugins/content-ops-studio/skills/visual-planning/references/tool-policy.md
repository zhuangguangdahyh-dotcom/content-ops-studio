# Tool policy

Six Visual tools are read-only. `content_ops_submit_visual_plan` writes only external-Home artifacts. `content_ops_finalize_visual_plan` is the sole open-world visual write and requires environment plus explicit confirmation gates. Never use raw Feishu, arbitrary Shell/File, image, Renderer or delete tools.
