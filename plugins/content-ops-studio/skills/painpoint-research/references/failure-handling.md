# Failure handling

Retain session state after every source, candidate, report, write and G2 checkpoint. A missing source, unsafe URL, mismatched hash, unknown evidence reference, invalid B confidence, forbidden D candidate, exact duplicate, stale item version, duplicate unique key or partial remote write blocks progression. Resume verified artifacts; do not restart blindly, create another Base, delete records, lower Schema strictness, or pad the result.
