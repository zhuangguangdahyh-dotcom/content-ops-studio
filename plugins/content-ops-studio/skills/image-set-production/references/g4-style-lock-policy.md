# G4 and Style Lock policy

Direction selection is not G4. G4 covers mechanical quality, visual quality and project fit, with final aesthetic authority held by the Operator. A valid checksum-bound G4 APPROVE creates `locked_rules`, `allowed_variations`, and `prohibited_deviations`. Lock language and invariants, not exact cover coordinates or a duplicated template.
