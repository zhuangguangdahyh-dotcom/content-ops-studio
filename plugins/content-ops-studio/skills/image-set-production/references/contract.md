# Contract

Inputs are exact approved Content/Copy/Visual versions, page roles, authorized assets, Pack/Profile bindings and capability evidence. Outputs are schema-valid local artifacts with Run ID, provenance, attempts, checksum and read verification. Candidate assets are never final assets. Writes remain under Project Home. Formal Feishu writes are owned by a later explicit workflow, not candidate production.
