# Phase 4B-R.1 cross-Run learning evidence

The controlled validation used fictional Project `PRJ-20990101-R1LR` in a dedicated Project Home outside the repository. It made no network call, Feishu write or image-generation request.

Run 1 bound `PVPFV-1 / COLD_START` to C-9001 and produced three content-derived directions. Their channel signature was not the former fixed AI / Pure Typography / Mixed slot sequence. Plan, confidence and ambiguity artifacts passed strict Schema validation and read verification.

A fictional explicit project preference then produced `VFE-PHASE4BR1-PROJECT-1` and unconfirmed Candidate `VRC-PHASE4BR1-PROJECT-1`. Only the explicit confirmation operation created active Rule `VR-PHASE4BR1-PROJECT-1@1` and `PVPFV-2 / LEARNING`; Pack and global preference remained unchanged.

Run 2 used different Content C-9002, loaded PVPFV-2, applied authentic-work-scene and low-saturation rules, and used a distinct composition from Run 1. A separate CURRENT_SET override applied a deep-charcoal direction while the active Profile checksum stayed unchanged.

Explicit revoke created disabled Rule version 2 and PVPFV-3. The next Run bound PVPFV-3 and did not apply the disabled rule; Run 2 remained bound to PVPFV-2. Unit evidence also verifies hashed forget tombstones without history deletion.

- Full local evidence: external Project Home `content-ops-studio-runtime-phase4br1`
- Repository-safe evidence: `reports/verification/phase-4b-r1-dynamic-visual-learning-evidence.json`
- Overall: `PASSED`
