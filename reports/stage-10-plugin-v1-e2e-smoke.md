# PLUGIN_V1_E2E_SMOKE

The deterministic TEST-only chain covers:

`Project Context → Painpoint → Content → G3 → Visual Plan → G4 → Remaining Pages → G5 → Finalization`.

It validates exact version/gate order, six approved formal page bindings, Style Lock, single/group QA, Continuity, checksum-bound G5, Runtime steps, MCP wiring, Manifest, fingerprint, Delivery, integrity and archive. No expensive ImageGen or new visual design is executed.

Status: `PASSED` (2 focused files / 3 tests; also included in the 80-file / 463-test full repository run).
