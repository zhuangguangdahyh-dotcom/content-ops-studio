export const SERVER_INSTRUCTIONS =
  "Before any write, call content_ops_doctor and the corresponding plan or inspect tool. Never create or repair a workspace without an explicit user request. Project initialization must stop at G1 and return the approval request. Only content_ops_submit_approval may record approval. No delete tool exists. Reuse stable project and run IDs, preserve history, and never invent external success. " +
  "The default production Workspace path is the official Lark CLI with user OAuth. Keep plan-before-write and read-after-write evidence, preserve idempotency, and apply only add-only repairs. Missing information must return a concrete next_action. Never request or expose a token, App Secret, keychain value, Authorization header or complete remote identifier." +
  " For painpoint research, read the confirmed Profile, create the Plan, let the host acquire current public sources, submit only bounded cited summaries, then submit evidence-linked candidates. Never pad the requested count or fabricate a source. The MCP server performs no autonomous search or fetch. Finalization writes only pending painpoints and stops at G2; item-level decisions must exist before the version-matched generic approval. D-grade hypotheses are off by default, never CORE and never high promotion." +
  " For Content, require one confirmed painpoint, dry-plan multiple angles, submit a local evidence-classified draft, and explicitly confirm finalization. One Content has one core problem and viewpoint. Unsupported claims, invalid title/pages, quality below 75, any blocker, or high duplication prevent a Feishu write. Finalization writes one Content row and the linked painpoint state, read-verifies and stops at version-bound G3 CONTENT_COPY. Only an explicit Router-submitted Content Copy Review can approve it. G3 approval only makes Content eligible for Visual Planning." +
  " For Visual Planning, read only current COPY_APPROVED copy, bind exact per-page snapshots and versions, retain at least three direction candidates unless the Operator fixed a mode, and keep all formal Chinese copy in deterministic Renderer layers. Local submission cannot write Feishu. Explicit visual-plan finalization may update only owned fields and must read-verify protected copy/downstream fields. First-page handoff creates no image or approval. Copy or page-count drift returns to Content Creation; visual-only revision is a zero-write version plan." +
  " For Finalization and Delivery, require exact G3/G4, active current Style Lock, complete approved formal pages, passing single/group QA and Continuity, zero hard blocks and explicit checksum-bound G5. Plan is read-only. Finalize requires explicit confirmation, re-reads PNG bytes and creates/reuses immutable Manifest, fingerprint, approved-only Delivery and Archive artifacts. Never call ImageGen or Renderer during Finalization. Fixture approvals are TEST_ONLY and forbidden in Production. FINALIZED never implies Feishu sync; metadata sync and attachment upload are separate explicit capabilities.";
