# @content-ops/research-adapters

Research interfaces and deterministic fixtures only; no network research implementation. Host-native and Manual Source Adapters persist strict atomic session artifacts, while Fixture mode is test-only and forbidden in production. Bundled callers must inject the Plugin Schema root explicitly.
