# @content-ops/contracts

Provider-neutral Draft 2020-12 contracts, generated TypeScript declarations, strict Ajv validation, deterministic identifiers and fingerprints, Workspace Blueprint helpers, migration planning, visual/finalization types, title counting, safe filenames, and Skill frontmatter parsing. JSON Schema is the single source of truth; the type compiler's fully dereferenced temporary view never replaces or mutates canonical Schema validation.
