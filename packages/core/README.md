# @content-ops/core

Provider-neutral data-driven state machines, G1-G5 approval validation, cross-state workflow invariants, safe temporary/in-memory Asset Stores, path/checksum/page naming utilities, visual/finalization validation, and history-preserving invalidation calculation. Pure evaluation performs no external writes or service calls; the temporary store writes only under a caller-supplied test root and never overwrites a file.
